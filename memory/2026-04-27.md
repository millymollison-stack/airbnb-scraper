# Daily Note — 2026-04-27

## Session Summary

Good productive session. All work on the Surf House Baja template (02-surfhousebaja-template).

### Done Today

1. **Spinner replacement** — Replaced sidebar's old `.sidebar-spinner` with `spinner-ring` class. Also updated `spinner-ring` border from `#f3f4f6` to `#d1d5db` for visibility on white backgrounds. Also replaced Home page Tailwind `animate-spin` spinner.

2. **Removed debug overlay** — Deleted `showDebug` state, `useEffect` D-key handler, debug data panel JSX, and all `.debug-data-*` CSS from OnboardingPopup.

3. **Booking modal textarea** — Added `.booking-modal-textarea` CSS to `index.css` with `width: 80%`, `display: block`, `box-sizing: border-box`, `padding`, `border`, `background`, `resize: vertical`. Does NOT affect onboarding popup textarea.

4. **Removed Admin Dashboard + My Property buttons** — Deleted from UserMenu.tsx and sidebar.css. Also removed the `{user.role === 'admin' && ( ... )}` wrapper. Note: had to fix orphan JSX from removal.

5. **Day picker lozenge CSS (REVERTED)** — Tried to add CSS to make date ranges look like a continuous lozenge on large screens by rounding only outer edges of start/end days. Made calendar numbers invisible. Reverted at 1:18am — removed all `.rdp-day_*` CSS. Calendar back to default.

6. **Fixed TypeScript error** — Orphan `{user.role === 'admin' && (}` left over from button removal → caused parse error at line 569. Fixed.

### Key Files Changed
- `src/components/UserMenu.tsx` — sidebar UX, removed admin buttons
- `src/components/OnboardingPopup.tsx` — removed debug overlay
- `src/components/sidebar.css` — removed sidebar-actions CSS
- `src/index.css` — booking-modal-textarea styles
- GitHub: commits `c806e27` (styling fixes + debug removal), `5eb8d18` (earlier UX fixes)

### Lesson Learned
- Day picker (react-day-picker) CSS: react-day-picker generates its own class names (`rdp-day_*`). We should not target `button` in global CSS as it strips all default day-picker button styling. The lozenge idea was good but needs more careful implementation with correct class names and !important overrides that don't break the day cell rendering. Abandoned for now.

### Current State
- All 5 servers running: 5174, 6905, 3099, 9103, 9000
- Template working well
- Sidebar clean, spinners consistent, no debug overlay
- Booking textarea styled at 80% width
