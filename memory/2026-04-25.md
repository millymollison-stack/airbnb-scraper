
### Afternoon session (13:00-13:46)
- Reverted tailwind preflight disable (it broke too many things)
- Scoped h1 base rule to `.sidebar-panel h1` in sidebar.css (no longer leaks to main site)
- Added bare h1 rule to index.css with white color for hero titles
- Made `.hero-title` and `.hero-title-price` use `!important` on all props in index.css so they always beat Tailwind preflight
- Removed Cancel button from UserMenu sidebar header actions
- Reordered: Edit/Save on left, X on right
- Made all sidebar header buttons (Edit, Save, X) use `val-color` (var(--brand))
- Fixed popup-textarea.editmode in Editmode.css + OnboardingPopup.css: set to 80% + border-radius 6px, removed 100%
- Added single definitive popup-textarea.editmode rule to index.css to ensure 80% always wins
- Tailwind preflight DISABLED then RE-ENABLED (caused too many regressions)
