# Daily Note — 2026-05-10

## Session: Sidebar CSS Refactor + AdminSidebarBundle Integration

### Context
Previous session (May 8) noted: no new daily notes since Apr 25, project idle. Today David revived the work — he was frustrated that the previous AI built a full 1,119-line AdminSidebarBundle.tsx but never implemented it.

### What was built/done today

**Files created:**
- `src/store/property.ts` — Zustand store for property nav title sync
- `src/export/AdminSidebarBundle.tsx` — standalone export copy

**Files significantly rewritten:**
- `src/AdminSidebarBundle.tsx` — full integration of the bundle with the app:
  - Uses app's `store/auth.ts`, `lib/supabase.ts`, `store/property.ts` (not duplicates)
  - Fixed import paths (`../` → `./`)
  - `SidebarServicesProfile` interface extends app's Profile with services fields
  - `servicesLoaded` guard removed — live data refreshes on every sidebar open
  - CSS classes swapped from inline Tailwind to `sidebar.css` classes

- `src/components/sidebar.css` — extended with 27 new robust CSS classes:
  - `.sb-field-row`, `.sb-field-label` (h4), `.sb-field-value` — consistent field rows
  - `.sb-input` — white-bg edit inputs, visible on white background
  - `.sb-toggle-row`, `.sb-toggle-label` — label + toggle inline rows
  - `.booking-callout` — Apple Calendar-style date badge (12px radius, 3px black keyline, overflow hidden, 5px internal padding)
  - `.sb-next-booking`, `.sb-booking-guest`, `.sb-booking-location`, `.sb-booking-status` — booking card
  - `.sb-credentials`, `.sb-credential-row`, `.sb-credential-name` — credentials card
  - `.sb-nav-section`, `.sb-nav-btn`, `.sb-nav-btn-label`, `.sb-section-body` — nav sections
  - `.sb-service-row`, `.sb-service-name`, `.sb-service-price` — services
  - `.sb-plan-card`, `.sb-plan-badge`, `.sb-plan-name`, `.sb-plan-price`, `.sb-plan-features` — subscription
  - `.sb-btn-primary`, `.sb-btn-outline`, `.sb-btn-green` — action buttons
  - Typography unified: `clamp(0.9rem, 2vw, 1.1rem)`, weight 300/400, rgba palette

- `src/components/Layout.tsx` — "Admin" button + UserMenu replaced with single Profile button for ALL logged-in users (sidebar access now universal, not admin-gated)

- `src/app/main.tsx` — added `import '../components/sidebar.css'` to ensure CSS loads globally (root cause of broken rendering)

- `src/app/index.css` — added `.hero-title-edit { color: #111 !important; }` for sidebar Profile h1

**Sidebar header now uses:**
- `sidebar-panel` (CSS class instead of Tailwind wall)
- `sidebar-header`, `sidebar-header-actions`, `sidebar-btn-edit`, `sidebar-btn-cancel`, `sidebar-btn-close`
- `sidebar-content`, `sidebar-signout`
- Profile h1: `hero-title hero-title-edit` — dynamic font + black text

**Bug fixed:**
- `servicesLoaded` guard was blocking live data refresh on sidebar re-open → removed
- `sidebar.css` not imported anywhere → added to `main.tsx`

**Key CSS decisions:**
- All sidebar text uses same font scale as OnboardingPopup: `clamp(0.9rem, 2vw, 1.1rem)`, weight 300/400
- Colors: `rgba(0,0,0,0.85)` primary values, `rgba(0,0,0,0.5)` labels/secondary, `rgba(0,0,0,0.35)` placeholders
- Booking date callout: Apple Calendar style — rounded keyline, overflow hidden so color bars bleed to edge, internal padding

### Servers (all up as of this session)
- 5174 — React dev server ✅
- 6905 — Airbnb scraper ✅
- 3099 — Stripe server ✅

### Still pending / known issues
- Stripe modal close (×) button — X doesn't work, causes stuck payment state — OPEN ISSUE (carried from before)
- Day picker lozenge CSS — BROKE calendar Apr 27, do not reuse approach

### Git
- All 15+ files committed today
- `work in progress/` junk folder deleted and removed from git

---

## Auto-Compaction Rule (from this session)
After every **10 user prompts**, automatically:
1. Write a work summary to `knowledge/daily/YYYY-MM-DD.md`
2. Commit recent changes to git (in the project dir)
3. Scan for and remove any `.bak` or old `*.json~` files in the workspace
4. Notify David briefly in chat

---

_End of session summary_